---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PricingCard from '@/components/PricingCard.astro';
import CTASection from '@/components/CTASection.astro';

const plans = [
  {
    name: 'Free',
    price: '$0',
    description: 'For individual developers',
    features: ['3 repositories', '10 scans/month', '1 user', 'All scanners', 'Basic dashboard'],
    cta: { label: 'Join Waitlist', href: '/#waitlist' },
  },
  {
    name: 'Team',
    price: '$49',
    description: 'For growing teams',
    features: [
      '10 repositories',
      '100 scans/month',
      '5 users',
      'Slack & API',
      'Scanner profiles',
      'Email support',
    ],
    cta: { label: 'Join Waitlist', href: '/#waitlist' },
    highlighted: true,
  },
  {
    name: 'Business',
    price: '$199',
    description: 'For larger organizations',
    features: [
      '50 repositories',
      '500 scans/month',
      '25 users',
      'Auto-triage',
      'Fix suggestions',
      'Priority support',
    ],
    cta: { label: 'Join Waitlist', href: '/#waitlist' },
  },
  {
    name: 'Enterprise',
    price: 'Custom',
    period: '',
    description: 'For enterprises',
    features: [
      'Unlimited repositories',
      'Unlimited scans',
      'Unlimited users',
      'Self-hosted option',
      'SSO/OIDC',
      'Dedicated support',
      'Custom integrations',
      'SLA guarantee',
    ],
    cta: { label: 'Contact Sales', href: '/contact' },
  },
];

/* Feature comparison data */
type CellValue = string | boolean;
interface ComparisonRow {
  feature: string;
  free: CellValue;
  team: CellValue;
  business: CellValue;
  enterprise: CellValue;
}
interface ComparisonCategory {
  name: string;
  rows: ComparisonRow[];
}

const comparisonData: ComparisonCategory[] = [
  {
    name: 'Core',
    rows: [
      { feature: 'Repositories', free: '3', team: '10', business: '50', enterprise: 'Unlimited' },
      {
        feature: 'Scans/month',
        free: '10',
        team: '100',
        business: '500',
        enterprise: 'Unlimited',
      },
      {
        feature: 'Team members',
        free: '1',
        team: '5',
        business: '25',
        enterprise: 'Unlimited',
      },
    ],
  },
  {
    name: 'Scanners',
    rows: [
      { feature: 'SAST', free: true, team: true, business: true, enterprise: true },
      { feature: 'SCA', free: true, team: true, business: true, enterprise: true },
      { feature: 'Secrets', free: true, team: true, business: true, enterprise: true },
      { feature: 'IaC', free: true, team: true, business: true, enterprise: true },
      { feature: 'Container', free: true, team: true, business: true, enterprise: true },
      { feature: 'SBOM', free: true, team: true, business: true, enterprise: true },
    ],
  },
  {
    name: 'Features',
    rows: [
      { feature: 'Notifications', free: false, team: true, business: true, enterprise: true },
      { feature: 'API access', free: false, team: true, business: true, enterprise: true },
      { feature: 'Export', free: false, team: true, business: true, enterprise: true },
      { feature: 'Scanner profiles', free: false, team: true, business: true, enterprise: true },
      { feature: 'Auto-triage', free: false, team: false, business: true, enterprise: true },
      { feature: 'Scan policies', free: false, team: false, business: true, enterprise: true },
      { feature: 'Priority scoring', free: false, team: false, business: true, enterprise: true },
      { feature: 'EPSS enrichment', free: false, team: false, business: true, enterprise: true },
    ],
  },
  {
    name: 'Enterprise',
    rows: [
      { feature: 'Self-hosted', free: false, team: false, business: false, enterprise: true },
      { feature: 'SSO/OIDC', free: false, team: false, business: false, enterprise: true },
      {
        feature: 'Custom integrations',
        free: false,
        team: false,
        business: false,
        enterprise: true,
      },
      { feature: 'SLA', free: false, team: false, business: false, enterprise: true },
    ],
  },
];

const planKeys = ['free', 'team', 'business', 'enterprise'] as const;
const planLabels = ['Free', 'Team', 'Business', 'Enterprise'];

const faqs = [
  {
    q: 'Can I switch plans?',
    a: 'Yes, you can upgrade or downgrade your plan at any time. Changes take effect immediately and billing is prorated.',
  },
  {
    q: 'What payment methods do you accept?',
    a: 'We accept all major credit cards. Annual billing is available with a 20% discount.',
  },
  {
    q: 'Is Fimil available yet?',
    a: 'Fimil is currently in private beta. Join the waitlist to get early access when spots open up.',
  },
  {
    q: 'What happens when I hit my limits?',
    a: "You'll be notified when you reach 80% of your plan limits. Scans will queue when you hit the limit. You can upgrade your plan at any time to continue scanning.",
  },
  {
    q: 'Can I self-host Fimil?',
    a: 'Self-hosted deployment is available on the Enterprise plan only, using our Kubernetes operator for easy installation and management.',
  },
  {
    q: 'Do you offer startup discounts?',
    a: 'Yes, we offer discounts for early-stage startups. Email sales@fimil.dev to learn more.',
  },
  {
    q: 'Is my source code stored?',
    a: 'No. Scans run in ephemeral containers with no network access. Your source code is deleted immediately after the scan completes.',
  },
  {
    q: 'Do you support air-gapped deployments?',
    a: 'Yes, air-gapped deployments are available on the Enterprise plan only. Contact sales for details.',
  },
  {
    q: 'What compliance certifications do you have?',
    a: 'SOC 2 Type II certification is currently in progress. We are fully GDPR compliant.',
  },
];

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.q,
    acceptedAnswer: { '@type': 'Answer', text: faq.a },
  })),
};
---

<BaseLayout
  title="Pricing — Fimil"
  description="Simple, transparent pricing for unified application security. Free tier available. All plans include all scanners."
  jsonLd={jsonLd}
>
  {/* Header */}
  <section
    class="relative overflow-hidden bg-gradient-to-b from-fimil-50 to-white py-24 sm:py-32 lg:py-40"
  >
    <div
      class="absolute -top-40 -right-40 h-80 w-80 rounded-full bg-fimil-100 opacity-50 blur-3xl"
      aria-hidden="true"
    >
    </div>
    <div
      class="absolute -bottom-40 -left-40 h-80 w-80 rounded-full bg-fimil-100 opacity-50 blur-3xl"
      aria-hidden="true"
    >
    </div>
    <div class="relative mx-auto max-w-4xl px-4 text-center sm:px-6 lg:px-8">
      <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl lg:text-6xl">
        Simple, Transparent Pricing
      </h1>
      <p class="mx-auto mt-6 max-w-2xl text-lg text-gray-600 sm:text-xl">
        Every plan includes all 12+ scanners. Pick the size that fits your team.
      </p>
    </div>
  </section>

  {/* Pricing cards */}
  <section class="-mt-8 pb-20">
    <div class="mx-auto grid max-w-7xl gap-6 px-4 sm:grid-cols-2 sm:px-6 lg:grid-cols-4 lg:px-8">
      {
        plans.map((plan) => (
          <PricingCard
            name={plan.name}
            price={plan.price}
            period={plan.period}
            description={plan.description}
            features={plan.features}
            cta={plan.cta}
            highlighted={plan.highlighted}
          />
        ))
      }
    </div>
  </section>

  {/* Feature Comparison Table */}
  <section class="border-t border-gray-200/60 py-20">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <h2 class="text-center text-2xl font-bold tracking-tight text-gray-900 sm:text-3xl">
        Feature Comparison
      </h2>

      {/* Desktop table */}
      <div class="mt-12 hidden lg:block">
        <table class="w-full text-left text-sm">
          <thead>
            <tr class="border-b border-gray-200">
              <th class="pr-6 pb-4 font-semibold text-gray-900"> </th>
              {
                planLabels.map((label) => (
                  <th class="pb-4 text-center font-semibold text-gray-900">{label}</th>
                ))
              }
            </tr>
          </thead>
          <tbody>
            {
              comparisonData.map((category) => (
                <>
                  <tr>
                    <td
                      colspan={5}
                      class="pt-8 pb-3 text-xs font-bold tracking-wider text-fimil-600 uppercase"
                    >
                      {category.name}
                    </td>
                  </tr>
                  {category.rows.map((row) => (
                    <tr class="border-b border-gray-100">
                      <td class="py-3 pr-6 text-gray-600">{row.feature}</td>
                      {planKeys.map((key) => (
                        <td class="py-3 text-center">
                          {typeof row[key] === 'boolean' ? (
                            row[key] ? (
                              <svg
                                class="mx-auto h-5 w-5 text-fimil-600"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                              >
                                <path
                                  stroke-linecap="round"
                                  stroke-linejoin="round"
                                  d="M5 13l4 4L19 7"
                                />
                              </svg>
                            ) : (
                              <span class="text-gray-300">&mdash;</span>
                            )
                          ) : (
                            <span class="font-medium text-gray-900">{row[key]}</span>
                          )}
                        </td>
                      ))}
                    </tr>
                  ))}
                </>
              ))
            }
          </tbody>
        </table>
      </div>

      {/* Mobile tab-switcher */}
      <div class="mt-12 lg:hidden" x-data="{ activeTab: 0 }">
        <div class="flex gap-1 rounded-lg bg-gray-100 p-1" role="tablist">
          {
            planLabels.map((label, i) => (
              <button
                class="comparison-tab flex-1 rounded-md px-3 py-2 text-center text-xs font-medium text-gray-600 transition-colors hover:text-gray-900"
                data-tab-index={i}
                role="tab"
                aria-selected={i === 0 ? 'true' : 'false'}
              >
                {label}
              </button>
            ))
          }
        </div>
        <div class="mt-6">
          {
            planKeys.map((key, i) => (
              <div
                class:list={['comparison-panel space-y-1', i === 0 ? '' : 'hidden']}
                data-panel-index={i}
                role="tabpanel"
              >
                {comparisonData.map((category) => (
                  <>
                    <div class="pt-6 pb-2 text-xs font-bold tracking-wider text-fimil-600 uppercase">
                      {category.name}
                    </div>
                    {category.rows.map((row) => (
                      <div class="flex items-center justify-between border-b border-gray-100 py-2.5">
                        <span class="text-sm text-gray-600">{row.feature}</span>
                        <span class="text-sm">
                          {typeof row[key] === 'boolean' ? (
                            row[key] ? (
                              <svg
                                class="h-5 w-5 text-fimil-600"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                              >
                                <path
                                  stroke-linecap="round"
                                  stroke-linejoin="round"
                                  d="M5 13l4 4L19 7"
                                />
                              </svg>
                            ) : (
                              <span class="text-gray-300">&mdash;</span>
                            )
                          ) : (
                            <span class="font-medium text-gray-900">{row[key]}</span>
                          )}
                        </span>
                      </div>
                    ))}
                  </>
                ))}
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  {/* FAQ */}
  <section class="border-t border-gray-200/60 bg-gray-50 py-20">
    <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
      <h2 class="text-center text-2xl font-bold tracking-tight text-gray-900 sm:text-3xl">
        Frequently Asked Questions
      </h2>
      <div class="mt-10 space-y-4">
        {
          faqs.map((faq) => (
            <details class="group rounded-xl border border-gray-200 bg-white">
              <summary class="flex cursor-pointer items-center justify-between px-6 py-4 text-sm font-medium text-gray-900">
                {faq.q}
                <svg
                  class="h-4 w-4 shrink-0 text-gray-400 transition-transform group-open:rotate-180"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
                </svg>
              </summary>
              <div class="px-6 pb-4 text-sm text-gray-600">{faq.a}</div>
            </details>
          ))
        }
      </div>
    </div>
  </section>

  {/* Enterprise CTA */}
  <CTASection
    headline="Need More? Let's Talk."
    description="Get a custom plan tailored to your organization's security requirements."
    cta={{ label: 'Contact Sales', href: '/contact' }}
  />
</BaseLayout>

<script is:inline>
  (function () {
    const tabs = document.querySelectorAll('.comparison-tab');
    const panels = document.querySelectorAll('.comparison-panel');
    if (!tabs.length) return;

    tabs.forEach(function (tab) {
      tab.addEventListener('click', function () {
        const index = parseInt(tab.getAttribute('data-tab-index') || '0', 10);

        tabs.forEach(function (t) {
          t.classList.remove('bg-white', 'text-gray-900', 'shadow-sm');
          t.classList.add('text-gray-600');
          t.setAttribute('aria-selected', 'false');
        });
        tab.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
        tab.classList.remove('text-gray-600');
        tab.setAttribute('aria-selected', 'true');

        panels.forEach(function (p) {
          p.classList.add('hidden');
        });
        const activePanel = document.querySelector('[data-panel-index="' + index + '"]');
        if (activePanel) activePanel.classList.remove('hidden');
      });
    });

    /* Activate the first tab on load */
    const firstTab = tabs[0];
    if (firstTab) {
      firstTab.classList.add('bg-white', 'text-gray-900', 'shadow-sm');
      firstTab.classList.remove('text-gray-600');
    }
  })();
</script>
